<?php
/**
 * PHPStan stub for the php-cli-tools function WP-CLI provides.
 *
 * @package Founders\Migration
 */

namespace cli;

/**
 * Asks a question on the terminal.
 *
 * @param string      $question Question.
 * @param bool|string $default  Default answer.
 * @param string      $marker   Marker after the question.
 * @param bool        $hide     Hide the input.
 * @return string
 */
function prompt( $question, $default = false, $marker = ': ', $hide = false ) {
	return '';
}
